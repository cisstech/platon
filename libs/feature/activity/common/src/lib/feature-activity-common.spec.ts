import { featureActivityCommon } from './feature-activity-common';

describe('featureActivityCommon', () => {
  it('should work', () => {
    expect(featureActivityCommon()).toEqual('feature-activity-common');
  });
});
