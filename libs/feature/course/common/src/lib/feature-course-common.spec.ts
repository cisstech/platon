import { featureCourseCommon } from './feature-course-common';

describe('featureCourseCommon', () => {
  it('should work', () => {
    expect(featureCourseCommon()).toEqual('feature-course-common');
  });
});
